generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgres"
    url      = env("DATABASE_URL")
}

model Users {
    id       Int     @default(autoincrement())
    fullname String?
    email    String  @unique
    password String
}
